{
  "name": "index.js",
  "version": "1.0.0",
  "description": "A server that use one api to return an image",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nimra",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
